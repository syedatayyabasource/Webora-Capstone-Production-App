# Leave blank for local development because Vite proxies /api to localhost:5000.
# For a separate frontend deployment, set this to your deployed API origin, e.g. https://api.example.com/api
VITE_API_URL=
