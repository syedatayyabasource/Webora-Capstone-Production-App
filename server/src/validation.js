import { z } from 'zod';
export const loginSchema=z.object({email:z.string().trim().email('Enter a valid email address.'),password:z.string().min(8,'Password must be at least 8 characters.'),role:z.enum(['ADMIN','TEAM_MEMBER','CLIENT'])});
export const clientSchema=z.object({name:z.string().trim().min(2,'Name is required.').max(80),email:z.string().trim().email('Enter a valid email.'),password:z.string().min(8,'Password must be at least 8 characters.')});
export const projectSchema=z.object({name:z.string().trim().min(2,'Project name is required.').max(100),description:z.string().max(500).optional(),client_id:z.coerce.number().int().positive('Select a client.'),status:z.enum(['PLANNING','IN_PROGRESS','REVIEW','COMPLETED']),progress:z.coerce.number().int().min(0).max(100),deadline:z.string().min(1,'Deadline is required.')});
export const taskSchema=z.object({title:z.string().trim().min(2,'Task title is required.').max(120),description:z.string().max(500).optional(),project_id:z.coerce.number().int().positive('Select a project.'),status:z.enum(['TODO','IN_PROGRESS','DONE']),priority:z.enum(['LOW','MEDIUM','HIGH']),due_date:z.string().min(1,'Due date is required.')});
export const teamMemberSchema=z.object({name:z.string().trim().min(2,'Name is required.').max(80),email:z.string().trim().email('Enter a valid email.'),password:z.string().min(8,'Password must be at least 8 characters.')});
